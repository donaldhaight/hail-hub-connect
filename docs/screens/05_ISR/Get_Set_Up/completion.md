# Completion event

- **Event:** certification granted; the ISR role is held.
- **Recorded as:** a role grant plus the wallet claim at certification (A1, still open).
- **Task closed:** none — `State + Need → Task` does not exist (A25). When it does, this is
  the first place a real task chain should begin: Day 01 through Day 10.
- **What the person sees next:** the ISR area — today an empty room (D1).

The honest statement: a person can be set up for a position that opens onto nothing. That is
the gap between this screen and `05_ISR/ISR_Home`, and it is the reason Epic 7 sits where it
does in the register.
