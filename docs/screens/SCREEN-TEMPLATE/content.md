# Content — default rendering

The copy as it stands when no variant is selected. Plain mood, intensity 1, class as
declared in `purpose.md`.

Alternative renderings are siblings in [`variants/`](variants/) and never replace this one.
