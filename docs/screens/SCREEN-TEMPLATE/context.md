# Required context

What must be known before this container can render at all.

| Needed | Source | If missing |
|---|---|---|
| | | |

Memory partition this screen reads from (ADR-016): anonymous session · personal file ·
role- or app-scoped. A partition is never crossed by inference — only by the person's act.
