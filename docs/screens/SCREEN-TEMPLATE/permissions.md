# Permitted roles

Authority is `Role + applicable Relationship + applicable Assignment`, evaluated
record-scoped. Not every actor needs all three. A container declares intent; it is never
itself the access check — the server re-checks.

| Role | Relationship required | Assignment required | May open | Notes |
|---|---|---|---|---|
| | | | | |

**Denied state:** what a person who fails this test sees. Never a blank page, never a lie.
